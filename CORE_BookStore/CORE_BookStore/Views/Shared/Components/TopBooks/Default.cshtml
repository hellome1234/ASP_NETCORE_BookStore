@model IEnumerable<BookModels>

    <div class="row">
        @foreach (var book in Model)
        {
            <div class="col-md-4">
                <partial name="_BookCard" model="book" />
            </div>
        }
    </div>
